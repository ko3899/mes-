# MES工厂管家 数据库配置

# 数据库类型: sqlite / mysql / postgresql
MES_DB_TYPE=sqlite

# MySQL/PostgreSQL 配置（当 MES_DB_TYPE 不为 sqlite 时生效）
MES_DB_HOST=localhost
MES_DB_PORT=3306
MES_DB_NAME=mes_factory
MES_DB_USER=root
MES_DB_PASS=

# SQLite 配置（默认）
# 数据库文件自动在 database/mes.db
