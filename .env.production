# MES工厂管家 环境配置（示例文件，生产环境请通过环境变量注入敏感信息）

# 服务配置
MES_HOST=0.0.0.0
MES_PORT=8080
MES_WORKERS=4

# 会话密钥：生产环境必须设置为强随机字符串，长度不少于 32 字节
# 示例生成：python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=

# 日志级别
LOG_LEVEL=INFO
